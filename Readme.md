# deepin-ppa

```bash
# import gpg
curl -s --compressed "https://rabenda.tk/deepin-ppa/repo-public.gpg" | sudo apt-key add -
# use
sudo curl -s --compressed -o /etc/apt/sources.list.d/rabenda-deepin-ppa.list "https://rabenda.tk/deepin-ppa/software.list"
```
